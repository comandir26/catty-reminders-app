name: CD - Deploy via Docker Compose

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy with Docker Compose
        run: |
          echo "Starting Docker Compose deployment..."
          
          ssh -p 2200 -o StrictHostKeyChecking=no vlad@course.prafdin.ru '
            mkdir -p /home/vlad/catty-compose
            cd /home/vlad/catty-compose
          '

          scp -P 2200 -o StrictHostKeyChecking=no docker-compose.yaml vlad@course.prafdin.ru:/home/vlad/catty-compose/
          
          ssh -p 2200 -o StrictHostKeyChecking=no vlad@course.prafdin.ru '
            cd /home/vlad/catty-compose
            
            echo "=== Authentication ==="
            echo "${{ secrets.DOCKER_PAT }}" | docker login ghcr.io -u comandir26 --password-stdin
            
            echo "=== Pulling Latest Images ==="
            docker compose pull --quiet
            
            echo "=== Starting Stack ==="
            docker compose up -d --remove-orphans
            
            echo "=== Waiting for Services ==="
            sleep 30
            
            echo "=== Checking Services ==="
            docker compose ps --all
            
            echo "=== Health Check ==="
            curl -f http://localhost:8181/ || echo "App might be still starting..."
            
            echo "Deployment completed!"
            echo "App is available at: https://app.volkovvs.course.prafdin.ru"
          '